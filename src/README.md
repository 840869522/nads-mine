# Drone Cybersecurity Lab

This project is a demo platform built with Next.js and TypeScript. It simulates drone attack and defense scenarios, provides learning quizzes, and manages various infrastructure elements. Most data is generated locally with Prisma and SQLite.

Run `npm install` and then `npm run dev` to start the development server.
